import 'package:flutter/material.dart';

class ScreenItemDetail extends StatelessWidget {
  const ScreenItemDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
