import 'package:flutter/material.dart';

class ScreenItemsList extends StatelessWidget {
  const ScreenItemsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
